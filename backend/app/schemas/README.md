# Schemas

Pydantic request/response models, one module per resource. Never expose
SQLAlchemy models directly over the API — every endpoint takes/returns a
schema defined here instead.

Empty for now — populated starting with Feature 1 (Authentication).
