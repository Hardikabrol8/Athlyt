# Models

SQLAlchemy ORM models, one module per table. `Base` and the shared
`UUIDPrimaryKeyMixin`/`TimestampMixin` live in `app/db/base.py`.

Empty for now — the first model (`User`) arrives with Feature 1 (Authentication).
